#  Adds volumes allowing local modifications to files inside the running container
version: '3.7'

services:
  test:
    volumes:
      - ./src:/var/www/html
  seed:
    volumes:
      - ./src:/var/www/html
  migrate:
    volumes:
      - ./src:/var/www/html
