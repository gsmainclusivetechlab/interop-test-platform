{
    "private": true,
    "scripts": {

        "lint-all": "prettier . --write",
        "lint-staged": "precise-commits",
        "precommit": "npm run precise-commits",

        "build": "docker-compose build",
        "docker-base": "docker-compose -f docker-compose.yml",
        "docker-ops": "npm run docker-base -- -f compose/ops.yml run",

        "dev": "npm run docker-base -- -f compose/expose-web.yml -f compose/mailhog.yml -f compose/network.yml -f compose/phpmyadmin.yml -f compose/volumes.yml up",
        "prod": "npm run docker-base -- -f compose/expose-web.yml -f compose/production.yml up -d --force-recreate ",
        "down": "npm run docker-base -- down",

        "has-db": "npm run docker-ops has-db",
        "backup": "npm run docker-ops backup",
        "seed": "npm run docker-ops seed",
        "migrate": "npm run docker-ops migrate",
        "test": "npm run docker-ops test && docker cp `docker-compose -f docker-compose.yml -f compose/ops.yml ps --all -q test  | head -n 1`:/var/www/results ./results"
    },
    "devDependencies": {
        "@prettier/plugin-php": "^0.14.2",
        "husky": "^4.2.5",
        "precise-commits": "^1.0.2",
        "prettier": "^2.0.5"
    }
}
