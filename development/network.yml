# Connect to an external docker network to allow communication with local services

version: "3.7"

services:
  php:
    networks:
      itp:

  web:
    networks:
      itp:
        aliases:
          - itp-core

networks:
  itp:
    driver: bridge
