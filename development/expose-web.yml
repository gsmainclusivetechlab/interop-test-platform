# Connect to an external docker network to allow communication with the test platform

version: "3.7"

services:
  web:
    ports:
      - "${HOST_WEB_PORT}:80"
  mailhog:
    ports:
        - '${HOST_MAILHOG_PORT}:8025'
